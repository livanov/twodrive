{
  "name": "lumen",
  "version": "1.0.0",
  "private": true,
  "description": "Lumen — a unified OneDrive photo timeline (single file PWA)",
  "scripts": {
    "test": "node tests/run.js"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
